[package]
name = "midi-studio-partitions"
version = "1.0.0"
description = "MIDI Studio et Lecteur de Partitions Tactile"
authors = ["MIDI Studio"]
edition = "2021"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
